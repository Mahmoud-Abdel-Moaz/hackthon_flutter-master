import 'package:flutter/material.dart';
import 'package:provider/provider.dart';

class TopRelatedScreen extends StatefulWidget {
  const TopRelatedScreen({Key? key}) : super(key: key);

  @override
  _TopRelatedScreenState createState() => _TopRelatedScreenState();
}

class _TopRelatedScreenState extends State<TopRelatedScreen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
